server:
  port: 9000
spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/HarshaSuranjith/microservices-example-config.git
          clone-on-start: true
